# 考勤系统部署指南（零基础 · 全程免费 · 不用敲命令）

这份指南带你从「没有账号」到「同事能打开网址用」，一共 4 步，全部图形界面点几下就行。
代码我已经帮你准备好并提交到本地 git 仓库了，你只需要完成「上传」和「部署」。

> 你本机的代码目录（等下要用）：
> `C:\Users\kairos zhang\WorkBuddy\2026-08-10-14-10-51\attendance-app`

---

## 第 1 步：注册 GitHub 账号（免费，只需邮箱）

1. 打开浏览器，访问 **https://github.com**
2. 点右上角 **Sign up（注册）**
3. 依次填：
   - 邮箱（用你常用的）
   - 设置密码
   - 取一个用户名（英文/数字，比如 `kairos-attendance`）
4. 点 **Create account（创建账号）**
5. 去你填的邮箱，点 GitHub 发来的验证邮件完成验证

✅ 到这里你有了 GitHub 账号，免费，没有任何费用。

---

## 第 2 步：把代码上传到 GitHub（用 GitHub Desktop，不用敲命令）

### 2.1 安装 GitHub Desktop
1. 打开 **https://desktop.github.com**
2. 点 **Download（下载）**，下完双击安装
3. 打开 GitHub Desktop，用第 1 步的账号登录

### 2.2 把本地代码加进来
1. 在 GitHub Desktop 顶部菜单：**File（文件） → Add Local Repository（添加本地仓库）**
2. 在弹出的窗口里，点 **Choose…（选择）**，找到并选中这个文件夹：
   `C:\Users\kairos zhang\WorkBuddy\2026-08-10-14-10-51\attendance-app`
3. 它会识别到这是一个 git 仓库，点 **Add Repository（添加仓库）**

### 2.3 发布到 GitHub
1. 窗口右上角会出现一个 **Publish repository（发布仓库）** 按钮，点它
2. Name（名称）填：`attendance-app`
3. Description（描述）可留空或填「考勤系统」
4. 下面有个 **Keep this code private（保持私有）** 的勾——**想保密就勾上**（只有你能看），不想勾也行
5. 点 **Publish（发布）**
6. 等进度条走完，代码就上传到 GitHub 了 ✅

> 如果你在这一步登录/发布时卡住，随时把截图发我，我帮你看着点。

---

## 第 3 步：注册并部署到 Railway（免费，用 GitHub 一键登录）

### 3.1 注册 Railway（用 GitHub 登录，不用再填资料）
1. 打开 **https://railway.app**
2. 点 **Login（登录）**，选择 **Continue with GitHub（用 GitHub 继续）**
3. 跳到 GitHub 授权页，点 **Authorize Railway（授权）**
4. 回到 Railway，账号就建好了（免费）

> 说明：Railway 免费额度可能要求绑一张银行卡做身份校验——**免费额度内不会扣费**，不用时可随时在设置里看账单。如果介意，可改用 **Render**（https://render.com，免费档不用绑卡，但空闲 15 分钟后会休眠，下次打开稍慢几秒），流程几乎一样：注册 → 连 GitHub 仓库 → 部署。

### 3.2 一键部署
1. 在 Railway 首页点 **New Project（新建项目）**
2. 选 **Deploy from GitHub repo（从 GitHub 仓库部署）**
3. 如果提示授权访问 GitHub，点 **Authorize（授权）**
4. 在仓库列表里选 **attendance-app**
5. Railway 会自动识别这是 Node 项目，启动命令自动填好 `node server.js`（不用改）
6. 点 **Deploy（部署）**
7. 等 1–2 分钟，状态变成 **✅ Deployed（已部署）** 就成功了

### 3.3 拿到网址
1. 在部署成功的项目页，找到 **Deployments / Settings** 里的 **Domain（域名）** 或 **Generate Domain（生成域名）**
2. 会生成一个类似 `https://attendance-app-xxxx.up.railway.app` 的网址
3. 点开它，就能看到考勤系统了 🎉

---

## 第 4 步：发给同事

把第 3.3 步拿到的那个网址（如 `https://attendance-app-xxxx.up.railway.app`）发到微信/邮件给同事。
他们直接点开就能用，**你们看到的是同一份数据**，谁录的大家都看得到。

---

## ⚠️ 重要：数据备份（必看）

Railway 免费容器的硬盘是「临时」的——平台重启或重新部署时，**数据可能被清空**。

两个保险做法（建议都做）：
1. **挂载持久盘**：在 Railway 项目里加一个 Volume，并设置环境变量 `DATA_DIR` 指向挂载路径（server.js 已支持这个变量）。
2. **定期导出备份**：在网页里点「导出 Excel」，把数据存到本地；万一清空，用「导入 Excel」恢复即可。

小团队日常用，建议你每周导一次 Excel 备份，省心。

---

## 本地先试跑（可选）

部署前想先在自己电脑上看看效果：
1. 进入目录，双击运行 `server.js`（或用命令行 `node server.js`）
2. 浏览器打开 **http://localhost:3000**

本机服务现在就在跑着，你可以先打开 `http://localhost:3000` 体验，确认没问题再走上面的部署流程。

---

## 常见问题

**Q：部署后打开是空白/报错？**
A：多半是环境变量或构建问题。Railway 默认不需要构建（零依赖）。如果页面打不开，在 Railway 的 Deploy Logs 里看红色报错，截图发我。

**Q：同事打不开网址？**
A：确认 Railway 项目状态是 Deployed（绿色），且 Domain 已生成。免费服务若长时间无人访问可能休眠，第一次打开等几秒。

**Q：想换回单机版（数据只存自己浏览器）？**
A：根目录的 `attendance.html` 就是原来的单机版，双击即用，无需部署。
