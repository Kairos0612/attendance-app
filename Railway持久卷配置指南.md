# Railway 持久卷配置指南（数据永久保存）

> 前提：你已在 Railway 部署了 `attendance-app`（线上地址 `https://attendance-app-production-aa6f.up.railway.app/`）。
> 目标：挂载一个持久卷（Volume），让考勤数据在重启/重部署后不丢。
> 费用：Hobby $5/月 + 1GB 卷 $0.25/月 ≈ **$5.25/月**（约 ¥38/月）。
>
> ⚠️ 为什么必须做这步：Railway 的容器硬盘是**临时**的，默认数据写在 `/app/data`（容器内部），服务重启或重新部署会被重置为空库。挂卷后数据写入持久卷，与容器生命周期解耦。

---

## 第 1 步：进入服务设置

1. 打开 https://railway.app ，进入你的项目 `attendance-app`
2. 点左侧的 **attendance-app 服务**（不是项目名，是里面的服务卡片）
3. 顶部标签切换到 **Settings**（设置）

---

## 第 2 步：挂载持久卷

1. 在 Settings 页往下找 **Volumes**（持久卷）区域
2. 点 **New Volume**（或 "Add Volume"）
3. 填写：
   - **Volume Name**：`data`（随意，字母）
   - **Mount Path**：`/data`（**必须是这个路径**，下一步要用）
   - **Size**：`1 GB`（考勤数据极小，1GB 足够，费用 $0.25/月）
4. 保存

> 挂载后，服务会自动重启一次以应用新挂载点。

---

## 第 3 步：设置环境变量

1. 仍在服务设置页，找 **Variables**（变量）区域
2. 点 **New Variable**
   - **Key**：`DATA_DIR`
   - **Value**：`/data`
3. 保存

> 后端 `server.js` 已内置逻辑：检测到 `DATA_DIR` 环境变量就往该目录写数据，否则写默认目录。这步把"卷挂载点"和"程序写入点"接上。

---

## 第 4 步：重新部署使配置生效

1. 顶部切到 **Deployments**（部署）标签
2. 点 **Redeploy**（或 "Deploy Latest"）重新部署一次
3. 等状态变 ✅ **Live**

> 重新部署后，程序会读取 `/data/attendance.json`。若该文件不存在，会自动新建空库（不会读回旧临时目录里的数据）。

---

## 第 5 步：验证持久化

1. 打开你的线上网址
2. 点「加载示例数据」→ 录入/编辑几条记录
3. **重新部署一次**（Deployments → Redeploy）
4. 刷新页面，数据**还在** = 持久卷生效 ✅
5. 若数据没了，回头检查：卷 Mount Path 是否 `/data`、变量 `DATA_DIR` 是否 `/data`、是否重新部署过

---

## 常见问题

**Q：之前临时目录里的数据还在吗？**
A：挂卷后切换到了 `/data`，旧数据留在已丢弃的临时目录，无法转移。由于你之前是空库（0 条），无影响。以后若换卷，先用「导出 Excel」备份再迁移。

**Q：卷能扩大吗？**
A：能，Settings → Volumes 里调 Size，$0.25/GB/月。考勤 1GB 够用几年。

**Q：试用额度用完不绑卡会怎样？**
A：服务暂停，数据**仍在卷里不丢**（卷独立于试用期）。绑卡后服务恢复即可。所以数据是安全的，只是服务暂停访问。

**Q：和 Render 比？**
A：Railway 卷持久化是成熟稳定方案；Render Free 不支持持久磁盘，需付费档（$7/月起）。要持久且省心，Railway 卷是当前最优解。
